import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

const FooterBrand = styled(Grid, {
  name: "FooterBrand",
  slot: "Root",
})(() => ({}));

export default FooterBrand;
